[project]
name = "drumbot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "raccoon>=1.0.0",
]

[project.scripts]
start = "src.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
