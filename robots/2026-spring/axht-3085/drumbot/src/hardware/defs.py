"""
===========================================================
 Project:   tobi-test
 Generated: 2026-02-20 19:40:57
===========================================================

Authors:
  • Tobias Madlberger (Scaffold / CLI Author)
  • RaccoonOS Development Team (Contributors)

⚠️ This file was auto-generated by the project scaffold template.
   You are free to modify it as needed. Regeneration may overwrite changes.
"""

from libstp import DigitalSensor, IRSensor, Motor, MotorCalibration, Servo
from libstp import IMU as Imu


class Defs:
    imu = Imu()
    button = DigitalSensor(port=10)
    front_left_motor = Motor(
        port=0,
        inverted=False,
        calibration=MotorCalibration(
            ticks_to_rad=1.8183330218875493e-05, vel_lpf_alpha=1.0
        ),
    )
    front_right_motor = Motor(
        port=1,
        inverted=True,
        calibration=MotorCalibration(
            ticks_to_rad=1.4309997498779744e-05, vel_lpf_alpha=1.0
        ),
    )
    front_left_ir_sensor = IRSensor(port=0)
    front_right_ir_sensor = IRSensor(port=1)
    drum_pusher_servo = Servo(port=3)
    drum_motor = Motor(
        port=2,
        inverted=False,
        calibration=MotorCalibration(ticks_to_rad=1.0, vel_lpf_alpha=1.0),
    )
    servo_help_motor = Motor(
        port=3,
        inverted=False,
        calibration=MotorCalibration(ticks_to_rad=1.0, vel_lpf_alpha=1.0),
    )
    drum_light_sensor = IRSensor(port=1)
    analog_sensors = [front_left_ir_sensor, front_right_ir_sensor, drum_light_sensor]


__all__ = ["Defs"]
