"""Auto-generated by Raccoon Toolchain (Tobias Madlberger / RaccoonOS Team)"""

from libstp import (
    AnalogSensor,
    DigitalSensor,
    ETSensor,
    IRSensor,
    Motor,
    MotorCalibration,
    Servo,
    ServoPreset,
)
from libstp import IMU as Imu


class Defs:
    imu = Imu()
    button = DigitalSensor(port=10)
    front_left_ir_sensor = IRSensor(port=1)
    front_right_ir_sensor = IRSensor(port=2)
    drum_light_sensor = IRSensor(port=0)
    wait_for_light_sensor = AnalogSensor(port=5)
    et_range_finder = ETSensor(port=3)
    front_left_motor = Motor(
        port=0,
        inverted=False,
        calibration=MotorCalibration(
            ticks_to_rad=1.857806209203324e-05, vel_lpf_alpha=1.0
        ),
    )
    front_right_motor = Motor(
        port=1,
        inverted=True,
        calibration=MotorCalibration(
            ticks_to_rad=1.577099347483164e-05, vel_lpf_alpha=1.0
        ),
    )
    drum_motor = Motor(
        port=2,
        inverted=False,
        calibration=MotorCalibration(ticks_to_rad=1.0, vel_lpf_alpha=1.0),
    )
    servo_help_motor = Motor(
        port=3,
        inverted=False,
        calibration=MotorCalibration(ticks_to_rad=1e-05, vel_lpf_alpha=1.0),
    )
    drum_pusher_servo = Servo(port=0)
    pom_remover_servo = ServoPreset(
        Servo(port=3),
        positions={"start": 158, "push_first_orange_pom_away": 0, "standby": 145},
    )
    lift_drums_servo = Servo(port=1)
    analog_sensors = [
        front_left_ir_sensor,
        front_right_ir_sensor,
        drum_light_sensor,
        wait_for_light_sensor,
        et_range_finder,
    ]


__all__ = ["Defs"]
